<%= form_with(model: bill_share) do |form| %>
  <% if bill_share.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bill_share.errors.count, "error") %> prohibited this bill_share from being saved:</h2>

      <ul>
        <% bill_share.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :bill_id, style: "display: block" %>
    <%= form.number_field :bill_id %>
  </div>

  <div>
    <%= form.label :member_id, style: "display: block" %>
    <%= form.number_field :member_id %>
  </div>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
